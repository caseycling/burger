<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
		<link rel="stylesheet" href="../../public/assets/css/burger_style.css" type="text/css" />
		<title>Eat-da-burger!</title>
		<script src="https://code.jquery.com/jquery.js"></script>
		<script src="../../models/burger.js"></script>
	</head>
	<body>
		{{{ body }}}
	</body>
	<script>
		$(function() {
			$("#devour").on("click", function() {
				console.log("hello world")
			})
			
			$("#submit").on("click", function() {
			//Prevent default from submit event
			event.preventDefault();

			var newBurg = {
				burgName: $("[name=burgInput]").val().trim(),
			}
			console.log(newBurg.burgName)

			//Send post request
			$.ajax("/api/burgers", {
				type: "POST",
				data: newBurg
			}).then(
				function() {
					console.log("Created new burger")
					//Reload page to display new burger
					//This will fire the root route, triggering previously built code
					location.reload();
				})
			})
		})

	</script>
</html>
